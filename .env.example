# Tasmota-OpenBK-MCP Configuration
# Copy this file to .env and fill in your values

# --- Network scanning range ---
# Define the IP range for device discovery
START_IP=192.168.0.1
END_IP=192.168.0.254

# Optional: CIDR range (overrides START_IP/END_IP if set)
# NETWORK_RANGE=192.168.0.0/24

# --- MQTT broker connection ---
MQTT_BROKER=192.168.0.101
MQTT_PORT=1883
MQTT_USER=
MQTT_PASSWORD=

# --- Server ports (defaults shown) ---
MCP_SSE_PORT=9101
REST_API_PORT=9102

# --- Server bind (default: 127.0.0.1) ---
# For Docker with --network host, set BIND_HOST=0.0.0.0 and ALLOW_PUBLIC_BIND=1
BIND_HOST=127.0.0.1
# ALLOW_PUBLIC_BIND=1

# --- Feature flags ---
IOT_SCAN_ENABLED=1

# --- Device cache directory ---
IOT_DATA_PATH=/app/data