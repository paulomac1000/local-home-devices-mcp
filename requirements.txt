# IoT MCP Server Dependencies
defusedxml>=0.7.1
fastmcp>=2.0.0,<4.0.0
requests>=2.28.0
# MQTT support (optional - server works without it)
paho-mqtt>=1.6.0
# Tuya LAN + Cloud support (optional - server works without it)
tinytuya>=1.15.0
starlette>=0.27.0
uvicorn>=0.22.0
